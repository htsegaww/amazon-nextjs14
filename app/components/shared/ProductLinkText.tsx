import React, { ReactNode } from "react";

const ProductLinkText = ({ children }: { children: ReactNode }) => {
  return <div className="text-[#017085]">{children}</div>;
};

export default ProductLinkText;
